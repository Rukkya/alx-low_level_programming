#include<stdio.h>

/**
 * main - prints " programming is like building a multilingual 
 * puzzle, followed by a newline"
 * return 0.
 */
int main(void)/** function to return string
		*values
		*/
{
	puts ("\"programming is like building a multilingual puzzle");
	return(0);
}
